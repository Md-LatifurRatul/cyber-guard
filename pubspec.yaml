name: cyber_guard
description: "Defense-in-Depth Content Protection Framework - Custom security with zero third-party dependencies."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.4
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^17.0.0

flutter:
  uses-material-design: true
